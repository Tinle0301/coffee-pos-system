// exports.js — CSV/PDF export of reports (admin only)

/**
 * @param {string} reportId
 * @returns {Promise<Blob>}
 */
export async function exportReportAsCsv(reportId) {}

/**
 * @param {string} reportId
 * @returns {Promise<Blob>}
 */
export async function exportReportAsPdf(reportId) {}
